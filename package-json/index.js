// accessing color package

const color=require('colors');
console.log("adarsh".blue)
   

// accessing chalk package
// for using chalk package we can useing not current version we can use 4.1.2 version of chalk
var chalk = require("chalk");
var a=0;
if(a==0)
{
    console.log(chalk.green("adarsh"));
}
else{
    console.log(chalk.red("gujrjar"));
}